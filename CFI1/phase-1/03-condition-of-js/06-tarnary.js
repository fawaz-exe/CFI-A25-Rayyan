var num = 0;

num > 0 ? (num == 0 ? console.log('Number is 0'): console.log('Number is postive')) : (num == 0)? console.log('Number is zero'): console.log('Number is negative');
condition ? true : (cond2 ? true : false) // else if ternary